# Database seeds

The Phase 2 seed is idempotent and runs from `backend`:

```powershell
.\.venv\Scripts\python.exe -m app.db.seed
```

The synthetic Ooty reserve introduced later must always carry a demonstration-data label.
