# WildTrack frontend

React, TypeScript, and Vite interface for WildTrack.

```powershell
npm.cmd install
npm.cmd run dev
npm.cmd run test
npm.cmd run build
```

The development server uses `VITE_API_BASE_URL` when supplied and otherwise connects to `http://localhost:8000/api/v1`.
