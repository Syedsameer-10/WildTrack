# Infrastructure

Production-safe Dockerfiles and deployment definitions begin in Phase 1. Hosted databases are configured through environment variables and are not embedded in production containers.

